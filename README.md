در این کد، **Selenium + Selenium-Wire** استفاده شده تا **ترافیک شبکه مرورگر** هنگام لود یک صفحه را **لاگ‌برداری** کند.
این کاریه که خود Selenium به‌تنهایی نمی‌تونه انجام بده، و Selenium-Wire به‌عنوان یک پروکسی داخلی این قابلیت را اضافه می‌کنه.

---

# 🔍 توضیح کامل کارکرد کد

### 1) **راه‌اندازی Chrome با Selenium-Wire**

```python
driver = webdriver.Chrome(seleniumwire_options={'verify_ssl': False}, options=chrome_options)
```

Selenium-Wire به‌جای WebDriver معمولی اجرا می‌شود و یک **پروکسی داخلی** روی مرورگر سوار می‌کند تا بتواند:

* درخواست‌های شبکه (Request)
* پاسخ‌ها (Response)
* هدرها
* Query Param ها
* بدنه (Body)

را شنود (Capture) کند.

---

# 2) **رفتن به صفحه هدف**

```python
driver.get("https://www.scrapingcourse.com/api/products")
```

مرورگر یک درخواست GET به `/api/products` می‌زند.
چون سایت API–Base هست، خروجی فقط JSON است.

---

# 3) **صبر برای کامل شدن ترافیک شبکه**

```python
time.sleep(5)
```

این مکث باعث می‌شود تمام Request/Response ها دریافت و توسط Selenium-Wire ثبت شوند.

---

# 4) **پرینت گرفتن تمام درخواست‌های شبکه**

```python
for req in driver.requests:
    if req.response:
        print(req.method, req.url, req.response.status_code)
```

اینجا Selenium-Wire کل ترافیکی که مرورگر هنگام باز کردن صفحه تولید کرده را لیست می‌کند.

اطلاعاتی که چاپ می‌شود شامل:

* نوع درخواست (GET / POST / OPTIONS)
* آدرس کامل (URL)
* کد وضعیت HTTP (200, 404, 500…)
* فقط درخواست‌هایی که response دارند

---

# 5) **بستن مرورگر**

```python
driver.quit()
```

---

# 📌 این کد چه کاری انجام می‌دهد؟

✔️ همه درخواست‌های شبکه مرورگر را لاگ می‌کند
✔️ برای تست API هایی که در سایت لود می‌شوند فوق‌العاده کاربردی است
✔️ برای تست‌های امنیتی، Performance، Debug و مشاهده رفتار واقعی مرورگر عالیه
✔️ بدون نیاز به DevTools یا Proxy خارجی (مثل Charles / Fiddler)

---

# 🎯 کاربرد عملی این کد در تست نرم‌افزار

### برای شما به‌عنوان QA / Automation Engineer:

🔸 تشخیص اینکه UI واقعاً چه API هایی را صدا می‌زند
🔸 مانیتور کردن اینکه API درست اجرا می‌شود یا نه
🔸 پیدا کردن ارورها (مثل 400 یا 500) که در UI نمایش داده نمی‌شوند
🔸 تحلیل Slow Request ها
🔸 بررسی درست بودن هدرها و توکن‌ها
🔸 Debug رفتار Authentication و Session
🔸 تست Smoke برای Availability سرویس‌ها

اگر خواستی همین کد را تبدیل کنم به نسخه پروفشنال‌تر (فایل لاگ، زمان درخواست، هدرها، بدنه و …) بگو تا سریع برات بنویسم.
